# Servidor
PORT=3333
NODE_ENV=development

# Banco de dados
DATABASE_URL="postgresql://syscontabel:syscontabel123@localhost:5432/syscontabel?schema=public"

# JWT
JWT_SECRET="altere-esta-chave-em-producao"
JWT_EXPIRES_IN="7d"

# URL do frontend (CORS) — em produção use a URL pública
# FRONTEND_URL=https://syscontabel-web.onrender.com

# Evolution API (WhatsApp) — opcional, pode rodar em outro servidor (ex. Hostinger)
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=sua-chave-aqui
EVOLUTION_INSTANCE=syscontabel

# Lembretes automáticos (dias antes do vencimento)
NOTIFICACAO_DIAS_ANTECEDENCIA=1

# Master da plataforma (painel /master — criar novas empresas SysContabel)
MASTER_EMAIL=master@syscontabel.local
MASTER_PASSWORD=master123456
