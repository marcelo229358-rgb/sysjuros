services:
  postgres:
    image: postgres:16-alpine
    container_name: syscontabel-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: syscontabel
      POSTGRES_PASSWORD: syscontabel123
      POSTGRES_DB: syscontabel
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U syscontabel -d syscontabel"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: syscontabel-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 3333
      NODE_ENV: production
      DATABASE_URL: postgresql://syscontabel:syscontabel123@postgres:5432/syscontabel?schema=public
      JWT_SECRET: ${JWT_SECRET:-altere-esta-chave-em-producao}
      JWT_EXPIRES_IN: 7d
    ports:
      - "3333:3333"

volumes:
  postgres_data:
